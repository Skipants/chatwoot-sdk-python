# Chatwoot SDK for Python

Python SDK for the Chatwoot API.

## Setup

```bash
# Install uv
curl -LsSf https://astral.sh/uv/install.sh | sh

# Install dependencies
uv sync

# Run tests
uv run pytest

# Type check
uvx ty check .

# Lint & format
uv run ruff check .
uv run ruff format .
```
